===== SKUF-CHAT — ОТЧЁТ ДЛЯ ПЕРЕХОДА =====
Версия: v1.15.0

## 0. КАК РАБОТАТЬ С ЭТИМ ПРОЕКТОМ (читать первым!)

Это продолжение проекта Skuf Chat. Прошлый чат стал слишком большим.
Считай v1.15.0 стабильной версией. НЕ ломай существующую рабочую архитектуру.
При изменениях:
1) Сначала анализируй текущий код.
2) Давай конкретные пошаговые действия для VS Code / PowerShell.
3) Пользователь — новичок. Команды давай ПО ОДНОЙ.
4) После каждого этапа жди результат (скрин или текст).
5) Не делай git add . вслепую — только конкретные файлы.
6) .gitignore НЕ трогать.
7) Коммиты: git add <конкретные файлы> → git status (проверка) → git commit → git push origin main → git tag vX.Y.Z → git push origin vX.Y.Z.
8) Всегда проверяем локально (npm start → http://localhost:3000) ПЕРЕД коммитом.

## 1. ТЕКУЩИЙ СТАТУС
- Production: https://skuf-chat.onrender.com
- GitHub: Gleb-design/skuf-chat (ветка main)
- Render: Live
- Локальный запуск: npm start → http://localhost:3000
- Стабильная версия: v1.15.0
- Хранение: RAM + Redis (Render Key Value, free tier)
  • История флудилки — в Redis (Sorted Set skuf:history, TTL 24ч)
  • Fallback в RAM, если Redis недоступен
  • Redis URL: Internal (redis://red-dajr9t9594qs73dauj50:6379) в Environment

## 2. СТЕК
- Backend: Node.js, Express, Socket.io, HTTP server
- Frontend: чистый HTML/CSS/vanilla JS
- Хранение: RAM + Redis (Render Key Value, free tier)
  • История флудилки — в Redis (Sorted Set, TTL 24ч), fallback в RAM
- Хостинг: Render (free Web Service). Может "засыпать" после простоя.

## 3. АРХИТЕКТУРА ЧАТОВ
- generalMessagesBox — общая флудилка (комната 'general')
- privateMessagesBox — приватные 1-на-1 (комнаты room_<id>_<id>)
- История общей флудилки — в Redis (Sorted Set skuf:history, TTL 24ч),
  fallback в RAM (globalMessagesHistory)
- Кастомные ники — в Redis (String skuf:nick:<sessionKey>), БЕЗ TTL.
  При подключении клиент шлёт sessionKey (из localStorage), сервер подтягивает ник.
  Fallback в RAM не реализован (для ников он не нужен — на проде всегда Redis).
- Статистика — в Redis: Hash skuf:stats:<YYYY-MM-DD> (messages, peakOnline)
  + Set skuf:stats:users:<YYYY-MM-DD> (уникальные sessionKey). TTL 7 дней.
  Fallback в RAM (ramStats + Set users), обнуляется при смене суток UTC.
- Очистка каждые 30 мин
- При подключении — новому пользователю отправляется история

## 4. РУЛЕТКА 1-НА-1
- Переменная waitingSkuf (глобальная)
- События: search_private, waiting, private_found, send_private_msg, leave_private, next_skuf, partner_disconnected
- При поиске пользователь выходит из general
- Автовозврат в general через ~3 сек после partner_disconnected

## 5. КНОПКИ В РУЛЕТКЕ
- btnNextSkuf ("🔄 Следующий скуф") — эмитит next_skuf
- btnCancelSearch ("❌ Выйти во флудилку") — эмулирует клик по btnGeneral

## 6. ЗВУКИ
- click.mp3 (своё сообщение) — volume 0.3
- beer.mp3 (чужое сообщение) — volume 0.5
- ambient.mp3 (фоновый звук гаража) — volume 0.08, loop=true, по умолчанию ВЫКЛ.
  Подробности — в разделе 13 → «Фоновый звук гаража (v1.15.0)»
- Кэшируются в service worker (CACHE_NAME = skuf-chat-v4)

## 7. RATE LIMIT
- 5 сообщений за 3 секунды (RATE_LIMIT_MAX=5, RATE_LIMIT_WINDOW_MS=3000)
- Функция isRateLimited(socket) в server.js

## 8. МОДЕРАЦИЯ (новое в v1.3.3)
- Стоп-лист рекламы: BAN_PATTERNS в server.js (t.me/, казино, ставки, крипта, 1xbet и т.п.)
- Функция containsAds(text) — если true, socket.emit('msg_blocked', {reason:'ads'})
- Антифлуд повторов: REPEAT_WINDOW_MS = 30 сек, REPEAT_MAX = 3
- Функция isRepeated(socket, text) — если true, socket.emit('msg_blocked', {reason:'repeat'})
- Мат НЕ фильтруется (это часть атмосферы!)
- Проверки в send_global_msg и send_private_msg — после isRateLimited, до создания messageData
- Клиент: обработчик msg_blocked показывает плашку

## 9. СЧЁТЧИК ОНЛАЙН
- Переменная onlineCount, broadcastOnlineCount() через io.emit('online_count')

## 10. ИНДИКАТОР "СКУФ ПЕЧАТАЕТ..."
- Клиент: input на messageInput, throttle 1.5 сек, socket.emit('typing')
- Сервер: if privateRoom → socket.to(privateRoom), иначе socket.to('general')
- Клиент: socket.on('partner_typing') → показывает #typingIndicator на 2 сек

## 11. OPEN GRAPH + PWA
- og:title, og:description, og:image (og-image.png 1200x630), Twitter Cards
- PWA: manifest.json, sw.js, CACHE_NAME = 'skuf-chat-v4' (актуально с v1.15.0)
- Иконка 512x512

## 12. НИКИ
- Собираются на сервере из skufNames + skufStatus + skufEmojis
- Примеры: "🍺 Кабан (главный по шашлыкам)"

## 13. АТМОСФЕРА (новое в v1.4.0)

### Общая архитектура тем
- Темы живут на .chat-area (фон) и .messages-wrapper (декорации)
- Переключение через классы theme-general / theme-private (add-before-remove!)
- ВАЖНО: декор по умолчанию display:none, показывается только при явном классе темы
- Структура DOM: .chat-area > .messages-wrapper > (.messages-container + .skuf-decor-элементы)
- .messages-wrapper имеет position:relative, overflow:hidden — якорь для декораций
- Шапка (.chat-header), input-area (.chat-input-area) и .private-controls имеют z-index:10 — поверх декораций

### Курилка (theme-general)
- Фон: кирпичная SVG-кладка + тёплое затемнение + 3 radial-gradient пятна света
- .skuf-decor.lamp1 (центр), .lamp2 (слева), .lamp3 (справа) — z-index:50 (поверх сообщений!), мерцание @keyframes lampFlicker
- .skuf-decor.smoke + .smoke2 — 2 дыма, анимация @keyframes smokeRise
- .skuf-decor.table — деревянный стол снизу
- УБРАНЫ: плакат, ящик (были, но мешали)
### Время суток (v1.8.0)
- JS определяет фазу по часам пользователя (client.js → getTimeOfDay / applyTimeOfDay)
- Фазы: утро (6-12), день (12-18), вечер (18-23), ночь (23-6)
- Классы: .chat-area.theme-general.time-morning / .time-day / .time-evening / .time-night
- Для каждой фазы — свой background-color + своя комбинация radial-gradient пятен
- Класс вешается на .chat-area и .messages-wrapper ПАРАЛЛЕЛЬНО с theme-general
- Обновляется раз в 5 мин (setInterval в client.js), чтобы переключиться, если юзер сидит долго

### Приват (theme-private)
- Фон: SVG-орнамент ковра + мягкое тёплое свечение (::before на .messages-wrapper)
- .skuf-decor.bar-counter — барная стойка (дерево+глянец, linear-gradient + ::before блеск)
- .skuf-decor.bottle1, .bottle2 — 2 бутылки-силуэта на стойке (bottom: 40px)
- .skuf-decor.glass — стакан по центру (bottom: 40px)
- .skuf-decor.smoke-p1, .smoke-p2 — 2 дыма
- Ковёр — оставили как был
### Фоновый звук гаража (v1.15.0)
- Файл: public/ambient.mp3 (сверчки + ветер, CC0, ~45 сек, зациклен)
- Кнопка 🔊/🔇 в шапке, слева от 🍺 (id="btnSound", класс .mini-sound-btn)
- По умолчанию ВЫКЛЮЧЕН. Юзер сам решает.
- Громкость: 0.08 (ambientSound.volume в client.js)
- Состояние хранится в localStorage ('skuf_ambient': 'on' | 'off')
- При загрузке: если 'on' — пробуем play(). Если браузер блокирует
  автозапуск — ждём первый клик по странице (kickstart, { once: true })
  и запускаем. Дальше работает нормально.
- Пауза на неактивной вкладке: document.visibilitychange → pause().
  Возврат: если ambientEnabled → applyAmbient().
- CSS: .mini-sound-btn (обычное — синее, .active — зелёное со свечением)
- Кэшируется в sw.js: CACHE_NAME = 'skuf-chat-v4'
### Приват — дополнение v1.7.0: Кот 🐈
- .skuf-decor.cat — контейнер 125×72px, справа на барной стойке (right: 26%, bottom: 40px)
- Внутри — <img src="/cat.svg"> (SVG-силуэт спящего кота, файл в public/)
- Анимация catBreathe — плавное «дыхание» через scaleY(1→1.03), transform-origin: center bottom
- Файл public/cat.svg — добавлен в v1.7.0

### Файлы
- style.css — все темы, декорации, keyframes
- client.js — функция addAllDecor() (вставляет курилочные + приватные декорации в .messages-wrapper)
- index.html — обёртка .messages-wrapper вокруг обоих .messages-container

## 14. СТРУКТУРА ПРОЕКТА
skuf-chat/
├── public/
│   ├── client.js
│   ├── index.html
│   ├── style.css
│   ├── sw.js
│   ├── manifest.json
│   ├── icon.png
│   ├── og-image.png
│   ├── cat.svg            ← новый (v1.7.0)
│   ├── click.mp3
│   ├── beer.mp3
│   └── ambient.mp3       ← новый (v1.15.0)
├── server.js
├── package.json
├── package-lock.json
├── report.txt             ← отчёт для передачи контекста
├── .gitignore

## 15. ИСТОРИЯ ВЕРСИЙ
- v1.0.0 — базовая версия
- v1.1.0 — кнопки "Следующий скуф" / "Выйти во флудилку"
- v1.1.1 — починены звуки
- v1.1.2 — rate limit 5/3с
- v1.2.0 — счётчик онлайн + favicon
- v1.2.1 — индикатор "Скуф печатает..."
- v1.3.0 — Open Graph + og-image.png
- v1.3.1 — починен "Следующий скуф"
- v1.3.2 — фикс плашки rate_limited (центрирование)
- v1.3.3 — модерация: стоп-лист рекламы + антифлуд повторов (3/30сек)
- v1.4.0 — атмосфера: тема курилки (кирпич+3 лампы+стол+2 дыма) и ламповый бар в привате (ковёр+стойка+2 бутылки+стакан+2 дыма)
- v1.4.1 — донат-кнопка 🍺 в шапке (пока заглушка через alert; реальная ссылка — позже, DonationAlerts под вопросом из-за проблем с выводом в РБ, CloudTips как альтернатива)
- v1.5.0 — Redis: история флудилки переехала из RAM в Render Key Value
  (Sorted Set, TTL 24ч, fallback в RAM при недоступности Redis)
- v1.5.1 — фикс Redis: tls отключается для Internal URL, maxRetriesPerRequest=null,
  убран username (Render ждёт только пароль)
- v1.6.0 — телевизор в курилке (мерцающий экран, свечение, антенна)
- v1.7.0 — кот в привате (SVG-силуэт спящего кота на барной стойке, анимация дыхания)
- v1.8.0 — время суток: фон курилки меняется по часам (утро/день/вечер/ночь),
  разные radial-gradient пятна света для каждой фазы
- v1.9.0 — кастомные ники: команда /nick <имя> в чате, валидация (2-20 символов,
  буквы/цифры/пробел/дефис/_), проверка на рекламу, системное сообщение о смене,
  обновление ника в шапке. Ник привязан к socket.id (сбрасывается при переподключении).
  Доступ пока свободный (без донат-кодов) — для теста и обкатки механики.
- v1.10.0 — сохранение ника между сессиями: клиент генерирует sessionKey
  (crypto.randomUUID) и хранит в localStorage; сервер по ключу skuf:nick:<sessionKey>
  читает/пишет ник в Redis. Ник переживает F5 и перезапуск сервера (в проде).
  Локально без Redis ник НЕ сохраняется (RAM-фолбэк для ников не делали — не нужен).
  Порядок инициализации: клиент шлёт init_session после события server_ready,
  которое сервер отправляет ПОСЛЕ регистрации всех обработчиков.
- v1.11.0 — мини-статистика в шапке: 📊 Сегодня: N сообщений · N скуфов · пик N.
  Метрики: сообщений за сутки (общая + приват), уникальных скуфов (по sessionKey),
  пик онлайна. Хранение — Redis (Hash skuf:stats:<YYYY-MM-DD> для messages/peakOnline,
  Set skuf:stats:users:<YYYY-MM-DD> для уникальных), TTL 7 дней. Fallback в RAM
  (объект ramStats, обнуляется при смене суток UTC). UI: плашка в шапке под счётчиком
  онлайна, обновление раз в минуту через socket.emit('get_stats') → 'stats_update'.
  Склонения русских слов: plural(n, 'сообщение', 'сообщения', 'сообщений').
  Проверено в проде: статистика переживает manual deploy (Redis работает).
- v1.12.0 — скуфский набор эмодзи: кнопка 😊 слева от поля ввода, панель 6×6
  с 36 смайлами. Набор: «быт» (🍺🍻🍢🎣📺🛋️🔧🥟🍖🧦🧖🏡), «напитки» (🥃🍷🍸),
  «реакции» (😎🤙🖕💪🤷🥴😴💀🔥😂), «классика ICQ» (🙂☹️😉😄😛😢😱😡),
  «особое» (👀🍑🤘). Без текстовых кодов — только панель. Вставка в позицию
  курсора через insertEmoji(). Toggle по кнопке, закрытие по клику вне.
  Файлы: public/index.html (разметка), public/style.css (стили панели),
  public/client.js (SKUF_EMOJIS, renderEmojiPanel, insertEmoji, toggleEmojiPanel).
- v1.13.0 — донат-кнопка 🍺: alert заменён на реальную ссылку DonationAlerts,
  открывается в новой вкладке (window.open с noopener). Файл: public/client.js
- v1.13.1 — три правки одним коммитом:
  • Фикс вёрстки input на узких экранах (iPhone SE 375px): кнопка «Отправить»
    больше не улетает за край. При ≤420px текст кнопки скрывается, остаётся
    только 🚀. Правки: .input-row (min-width:0 на input, flex-shrink:0 на кнопках),
    .emoji-btn (компактнее), медиа-запрос @media (max-width: 420px).
    Файлы: public/style.css, public/index.html.
  • Админ-режим для статистики: плашка «📊 Сегодня: ...» видна ТОЛЬКО по
    секретному URL. Клиент читает ?admin=СЕКРЕТ, сохраняет флаг в localStorage
    и чистит URL через history.replaceState. Обычные юзеры статистику вообще
    не запрашивают (requestStats возвращает early). Файлы: public/client.js,
    public/index.html (класс hidden по умолчанию), public/style.css
    (.stats-counter.hidden { display: none }).
    ⚠️ Секрет: skuf-admin-2026 (задан в client.js → ADMIN_SECRET).
  • Усилен дым во всех 4 местах (2 в курилке + 2 в привате): размер +40%,
    opacity поднята с 0.40 до 0.55, blur уменьшен с 3px до 2px, подъём
    увеличен с -60 до -85px. Правки: .skuf-decor.smoke/smoke2/smoke-p1/smoke-p2
    + keyframes smokeRise. Файл: public/style.css.
- v1.14.0 (прод-фикс) — вынос админ-секрета в env:
  • В server.js добавлен ADMIN_API_SECRET (из process.env). Без него
    /admin/* эндпоинты отключены. Раньше был хардкод 'skuf-admin-2026',
    из-за чего на проде с новым секретом всё падало в forbidden.
  • На Render → Environment задан ADMIN_API_SECRET (значение skuf-prod-*).
    ⚠️ Хранить в секрете, не коммитить в git.
  • Локально секрет живёт в .env (в .gitignore).
  • Временный /admin/debug удалён после отладки.
  • Проверено на проде: /admin/list-codes и /admin/add-code работают.

## 16. ИДЕИ НА ДАЛЬШЕ (по приоритету)

### МОНЕТИЗАЦИЯ (обсуждено, к реализации)
1. **Донат "на пиво"** — кнопка 🍺 в шапке → ✅ СДЕЛАНО в v1.13.0
   - ✅ v1.4.1: кнопка 🍺 добавлена в шапку (заглушка alert)
   - ✅ v1.13.0: реальная ссылка DonationAlerts, открывается в новой вкладке
   - ⚠️ Напоминание: у DonationAlerts проблемы с выводом в РБ
     (нужны VISA конкретных банков: Технобанк, МТБанк, БНБ, BSB и др.)
   - 💡 Если начнутся траблы с выводом — переехать на CloudTips
     (замена = 1 строка DONATE_URL в public/client.js)
2. **БД (Redis)** — ✅ СДЕЛАНО в v1.5.0–1.5.1
   - Render Key Value (free tier), Internal URL
   - Основа для: persistent session ID, кастомных ников, VIP-статусов
3. **Кастомные ники** — донат → код → ввод кода в чате → ник меняется
   - ✅ v1.9.0: базовая механика (команда /nick, валидация, системное сообщение,
     обновление ника в шапке).
   - ✅ v1.10.0: сохранение ника в Redis по sessionKey (localStorage-ключ).
     Ник переживает F5 и перезапуск сервера в проде.
   - ⏳ Осталось: генерация донат-кодов + проверка кода перед сменой ника.
     Сейчас /nick доступен свободно — без оплаты. Когда будет код — просто
     обернём saveNick в проверку «код валиден И не использован».
4. **VIP-статусы** — значок + приоритет в очереди рулетки

### АНГЛИЙСКАЯ ВЕРСИЯ (обсуждено, на подумать)

**Зачем:** открыть доступ к мировой аудитории (Reddit r/CasualConversation,
r/InternetIsBeautiful, Indie Hackers, Product Hunt, Hacker News).
Русскоязычная аудитория ограничена, англоязычная — в разы больше.

**Варианты реализации (от простого к сложному):**

1. **Двуязычный интерфейс (i18n) — простой старт.**
   - Переключатель языка в шапке (🇷🇺 / 🇬🇧).
   - Все тексты интерфейса (кнопки, плейсхолдеры, системные сообщения)
     выносятся в отдельный файл локализации (например, public/i18n.js).
   - Язык сохраняется в localStorage, применяется при загрузке.
   - Чат ОБЩИЙ — русские и англичане в одной комнате, каждый видит
     интерфейс на своём языке. Сообщения пользователей, разумеется,
     на языке автора (тут без перевода).
   - Плюс: просто, дёшево, не надо трогать серверную логику.
   - Минус: языковой барьер в чате — русский и англичанин не поймут друг друга.

2. **Разделение на две комнаты: ru и en.**
   - При входе выбор языка → попадаешь в свою комнату ('general_ru' / 'general_en').
   - Рулетка работает внутри языковой группы (русский ищет русского).
   - История — отдельная для каждой комнаты (ключ в Redis: skuf:history:ru / :en).
   - Статистика — общая или раздельная (обсудить).
   - Плюс: нет языкового барьера, каждый общается на своём.
   - Минус: критическая масса пользователей делится на 2, в каждой комнате
     онлайн меньше. На старте может быть пусто.

3. **Гибрид: общая комната + языковые теги.**
   - Один общий чат, но у каждого сообщения есть тег языка.
   - Фильтр в UI: «показать только 🇷🇺» / «только 🇬🇧» / «все».
   - Рулетка — по выбору языка.
   - Плюс: и общая масса, и разделение.
   - Минус: сложнее всего в реализации.

**Что рекомендую:**
- СНАЧАЛА — Вариант 1 (i18n + переключатель). Минимум работы, сразу видно,
  есть ли интерес от англоязычных.
- ЕСЛИ пойдёт — Вариант 2 (раздельные комнаты) или 3 (гибрид).

**Технические заметки:**
- Все системные сообщения на сервере (бот, rate_limited, msg_blocked,
  nick_error, partner_disconnected и т.д.) — вынести в i18n-словарь.
- На сервере тоже понадобится i18n: у сокета хранить preferredLang, брать
  перевод оттуда. Либо сервер отдаёт ключ ('rate_limited'), а клиент
  сам переводит (проще — весь перевод на клиенте).
- OG-теги, title, description — дублировать на английском для en-аудитории
  (отдельные URL /en или query ?lang=en — обсудить с SEO-точки зрения).
- Дизайн-атмосфера (курилка, гараж) — оставить как есть, она универсальна
  (своего рода «русский колорит» может быть фишкой для иностранцев).

### ПРИВЛЕЧЕНИЕ АУДИТОРИИ (обсуждено)
1. SEO — title/description с "анонимный чат без регистрации"
2. Пикабу — пост в "Лигу гаража"/"Лигу рыбаков"
3. Telegram-чаты тематические — общаться, потом упоминать
4. Indie Hackers — пост про реализацию (CSS-лампы, атмосфера)

### ФИЧИ (из старого бэклога)
1. Статусы собеседника (развить typing до "в гараже/отошёл/уснул")
2. ✅ Простая статистика (v1.11.0) — сделана в минимальном виде: сообщений
   за сутки, уникальных скуфов, пик онлайна. Показывается в шапке.
   ⏳ Идеи на развитие: топ-5 активных за день, статистика по часам,
   общий счётчик за всё время, отдельная страница /stats.
3. Redis / PostgreSQL — если проект вырастет

### НОВЫЕ ИДЕИ ПО АТМОСФЕРЕ (обсуждено)
- ✅ Мерцающий телевизор в курилке — СДЕЛАНО в v1.6.0
- ✅ Кот в привате 🐈 — СДЕЛАНО в v1.7.0
- ✅ Смена времени суток — СДЕЛАНО в v1.8.0
- ✅ Фоновый звук гаража (сверчки + ветер) — СДЕЛАНО в v1.15.0
  (подробности — в разделе 13). Идеи на развитие: выбор звука
  (сверчки / радио / дождь) через отдельную мини-панель.
- Пиксельный шрифт для ников
- ✅ Скуфский набор эмодзи (v1.12.0) — панель на 36 смайлов в 5 категориях.
  ⏳ Идеи на развитие: вкладки категорий в панели (Быт / Реакции / ICQ / Особое),
  недавно использованные смайлы, расширение набора.

## 17. ОСОБЕННОСТЬ RENDER
Free Web Service засыпает после простоя. Первый запрос может грузиться 20-30 сек.
Free PostgreSQL удаляется через 30 дней — для монетизации не подходит!
Render Key Value (Redis) — free tier, подходит.

## 18. ВАЖНЫЕ ФАЙЛЫ (для быстрого контекста)
- server.js — основной сервер (Socket.io, rate limit, модерация, счётчик, typing, next_skuf)
- public/client.js — вся клиентская логика + addAllDecor() + переключение тем
- public/index.html — вёрстка + OG-теги + обёртка .messages-wrapper
- public/style.css — стили, темы (курилка/приват), декорации, keyframes
- public/sw.js — service worker

## 19. ТЕКУЩИЕ ПРАВИЛА ПО ТЕМАМ (важно, чтобы не сломать!)
- Темы: .chat-area.theme-X + .messages-wrapper.theme-X
- Переключение: СНАЧАЛА add новый класс, ПОТОМ remove старый (add-before-remove)
- .skuf-decor по умолчанию display: none (чтобы не мигало при загрузке)
- Показ декораций: .messages-wrapper.theme-X .skuf-decor.YYY { display: block }
- Лампы поверх сообщений: z-index: 50
- Шапка/input-area/private-controls: z-index: 10
- .messages-wrapper: position: relative, overflow: hidden
- .messages-container: height: 100%, z-index: 3

===== КОНЕЦ ОТЧЁТА =====